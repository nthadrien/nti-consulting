import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
    site: 'http://https://nthadrien.github.io/nti-consulting/',
    // base: '/nti-consulting/',
});